<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.mapper.BoardMapper">

	<!-- 게시글 전체 조회 -->
	<select id="getAllBoard" resultType="BoardDto">
		SELECT *,
		       (SELECT Count(*)
		        FROM   comment
		        WHERE  board.bno = comment.bno) count
		FROM   board
		ORDER  BY bno DESC 
	</select>

	<!-- 게시글 상세 조회 -->
	<select id="searchByNo" parameterType="int" resultType="BoardDto">
		SELECT *
		FROM   board
		WHERE  bno = #{bno} 
	</select>

	<!-- 사용자 아이디로 검색하여 조회한다. id는 repo클래스의 메소드 이름과 일치시킨다. -->
	<select id="searchByNickName" parameterType="string" resultType="BoardDto">
		SELECT *
		FROM   board
		WHERE  nickname LIKE CONCAT('%', #{nickname}, '%') 
	</select>

	<!-- 게시글 제목으로 검색하여 조회한다. -->
	<select id="searchByTitle" parameterType="string" resultType="BoardDto">
		SELECT *
		FROM   board
		WHERE  title LIKE CONCAT('%', #{title}, '%') 
	</select>

	<!-- 게시글 말머리로 검색하여 조회한다. -->
	<select id="searchByCategory" parameterType="string" resultType="BoardDto">
		SELECT *
		FROM   board
		WHERE  category = #{category} 
	</select>

	<!-- 게시글 조회수 증가 -->
	<update id="updateHit" parameterType="int">
		UPDATE board
		SET    hit = hit + 1
		WHERE  bno = #{bno} 
	</update>

	<!-- 게시글 등록 -->
	<insert id="insert" parameterType="BoardDto">
		INSERT INTO board(title, category, nickname, content, date, hit) 
		VALUES (#{title}, #{category}, #{nickname}, #{content}, now(), 0) 
	</insert>

	<!-- 게시글 수정 -->
	<update id="update" parameterType="BoardDto">
		UPDATE board
		SET    title = #{title},
		       category = #{category},
		       nickname = #{nickname},
		       content = #{content}
		WHERE  bno = #{bno} 
	</update>

	<!-- 게시글 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE FROM board
		WHERE  bno = #{bno} 
	</delete>

</mapper>

